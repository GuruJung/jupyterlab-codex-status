{
  "jupyter.lab.setting-icon": "ui-components:terminal",
  "jupyter.lab.setting-icon-label": "Codex terminal status",
  "title": "Codex Terminal Status",
  "description": "Polling behavior for Codex terminal state.",
  "type": "object",
  "properties": {
    "pollIntervalMs": {
      "title": "Poll interval (milliseconds)",
      "description": "How often to request terminal state after a successful response.",
      "type": "number",
      "minimum": 500,
      "maximum": 10000,
      "default": 1000
    }
  },
  "additionalProperties": false
}

